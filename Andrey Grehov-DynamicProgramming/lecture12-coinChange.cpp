#include <bits/stdc++.h>

using namespace std;

/*
Problem:
	Coin change

	Given an unlimited supply of coins of given denominations(1,2,5,10),
	find the total number of ways to make a change of size n.
      1 + 1 + 1 + 1
     /
    4 -- 3 + 1
     \
      1 + 3
--> 3 ways

    1. Define objective function:
    f(i) is the total number of ways to make a change of size i
    2. Identify the base cases:
    f(0) = 1;
    f(1) = 1;
    f(2) = 1
 */
// coinChange(4)

int main() {
    cout << "Hello, World!";
    return 0;
}